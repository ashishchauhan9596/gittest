```bash
aliases are defined in ~/.bash_aliases


add alias in ~/.bash_aliases


syntax : alias <trigger>='<command>'


for e.g 

alias say_hello='echo "Hello"'


===========================================================================

save changes :

if using nano -> ctrl + x -> (y / n) enter

===========================================================================

TO reflect changes:

1. either close or reopen terminal 

OR

2. run -> source ~/.bashrc
===========================================================================
```